default:
  description: 'Development task'
  tasks:
    - 'clean:dev'
    - 'symlink:dev'
    - 'scsslint:all'
    - 'jshint:all'
    - 'sass:dev'
    - 'browserify:dev'
    - 'watch'

build:
  description: 'Build the frontend assets for deployment'
  tasks:
    - 'clean:build'
    - 'symlink:build'
    - 'scsslint:all'
    - 'jshint:all'
    - 'sass:build'
    - 'browserify:build'
    - 'ngAnnotate:build'
    - 'uglify:build'
